import React from 'react'

interface Props { }

function FooterLogo(props: Props) {
    const { } = props

    return (
        <div className='brightness-0 invert'>
            <img src='/images/logo/logo.svg' className='' alt='Shortly' />
        </div>
    )
}

export default FooterLogo
